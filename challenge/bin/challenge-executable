# TODO: Move this over to using PEX wrapper for webapp itself with entrypoint. Meantime, use gunicorn pex.
# TODO: PEX should be generated by build but this the PEX that is generated needs to be for multiple platforms (OSX, Linux)
pex -r challenge/requirements.txt -c gunicorn -o challenge/bin/gunicorn.pex
challenge/bin/gunicorn.pex -w 4 -b 0.0.0.0:7777 challenge.webapp:app
